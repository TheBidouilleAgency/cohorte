# Versioned prompts

Provider prompts are stored here once a workflow phase is implemented. Every attempt records the
artifact hash. There is deliberately no provider prompt in the deterministic core release.
