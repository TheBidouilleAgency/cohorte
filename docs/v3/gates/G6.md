# Gate G6 — release readiness

The V3 release workflow now delegates to `.github/workflows/ci.yml` through `workflow_call` and publishes the
immutable staged package produced by `scripts/build.ts`, preserving the `publish.yml` filename and the
`npm-publish` environment.

Verified locally:

- `pnpm ci:local` — all 14 configured local jobs green, including the seven legacy V2 suites;
- immutable build and `pack-check` — staged tarball installs and both entries run;
- 12 acceptance scenarios plus the discovery canary — collected and green on the immutable build;
- crash recovery — 10 simulated crash/restart cases green, with the full SQLite/journal/lease assertions;
- security path boundaries and packaged-CLI dogfood — green;
- schema compatibility — 24 schemas and 99 golden instances valid.

Still human-operated: the real Pi/live-provider smoke and the real Cohorte-on-Cohorte dogfood run.
