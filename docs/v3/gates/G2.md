# G2 — Wave 2 integration (retroactive final-tree audit)

This report records the Wave 2 integration evidence on the completed V3 tree. The original staged Wave 2
checkpoint was not preserved in the workspace; the commands below were rerun against the final tree and are
therefore evidence of the delivered behavior, not a claim that the historical wave was captured byte-for-byte.

## Evidence

- Policy, path, command, redaction, HMAC approval, tool-host and accounting contracts are present under
  `packages/security`, `packages/tools` and `packages/core`.
- The SQLite/fake-runtime skeleton and its crash/replay assertions are in
  `tests/integration/skeleton/engine-sqlite-fake.itest.ts`.
- `ci:local` passed lint, typecheck, 205 unit files / 3570 tests and 224 integration tests.
- The packaged CLI security and permission suites passed in the same run.

## Scope note

The plan names a dedicated `tests/integration/skeleton/hands.itest.ts`. That historical file is not present in
the final tree; its surviving evidence is split across the real SQLite/fake-runtime skeleton, tool-host/security
unit suites and the built-CLI permission scenarios. This is a traceability deviation, not a claim that a separate
hands fixture was retained.

## Exit evidence

```text
ci:local: integration
Test Files  24 passed (24)
Tests       224 passed (224)
schema-compat --self: 24 schema(s) compile under ajv 2020 strict, 99 golden instance(s) validate
```

The final immutable packaging build and `pack-check` also passed. No historical G2 checkpoint is claimed.
