# G3 — Wave 3 orchestration (retroactive final-tree audit)

This is a retroactive audit of the final V3 tree. It documents current evidence for orchestration, worktrees,
phase execution, integration and project-model behavior; it does not fabricate a historical checkpoint for the
wave.

## Evidence

- Supervisor incarnation accounting and runtime-event mapping are covered by the core supervisor tests.
- Worktree allocation, branch/base tracking and release are covered by the worktree service tests.
- TEST outcomes carry check results and distinguish failed checks from environmental errors.
- The built CLI dogfood and acceptance runs exercise the composed phase path and immutable integration tree.

## Scope note

The plan names `tests/integration/skeleton/pipeline.itest.ts`; the final tree instead retains the equivalent
composition evidence in the SQLite skeleton plus the built-CLI E2E/dogfood suites. The named historical pipeline
fixture and its wave-specific checkpoint were not preserved, so this report does not represent them as present.

## Exit evidence

```text
ci:local: unit       -> 205 files, 3570 passed, 1 skipped
ci:local: integration -> 24 files, 224 passed
ci:local: dogfood    -> 7 files, 7 passed
ci:local: acceptance -> 13 files, 13 passed
```

The final tree's `git diff --check`, layer check and TypeScript test compilation also passed.
