# G5 — Hardening (retroactive final-tree audit)

This report consolidates the hardening evidence available in the final V3 tree. It is a current-state audit,
not a reconstructed historical binary checkpoint.

## Evidence

- Crash recovery, security boundaries, packaged CLI behavior, migrations and schema compatibility are wired into
  the local CI matrix.
- Schema compatibility passed 24 schemas and 99 golden instances under AJV 2020 strict mode.
- Pi conformance passed 12 files and 307 tests.
- Dogfood D1/D3/D4/D5/D6 passed 7/7; acceptance passed 13/13.
- The unit timeout was raised to 30 seconds for the complete matrix because packaged-CLI and schema fixtures
  legitimately exceed Vitest's 5-second default under load; no assertions were weakened.

## Exit evidence

```text
ci:local: schema-compat -> 24 schema(s), 99 golden instance(s)
ci:local: pi-latest    -> 12 files, 307 passed
ci:local: dogfood      -> 7 files, 7 passed
ci:local: acceptance   -> 13 files, 13 passed
```

The final-tree hardening gate is green. A retained historical G5 checkpoint is not claimed.
