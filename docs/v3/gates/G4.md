# G4 — Built CLI integration (retroactive final-tree audit)

The final V3 tree was verified through immutable builds rather than through a retained historical G4 snapshot.
This report is explicit about that distinction.

## Evidence

- `scripts/build.ts` produced immutable `.build/ci`, `.build/ci-e2e`, `.build/ci-dogfood` and
  `.build/ci-acceptance` builds with the agent-host self-test green.
- `pack-check` validated the staged package and both packaged entries.
- Built-CLI E2E covered happy path, demo, review/fix, frontend/backend, unknown project, provider faults,
  vulnerability flow and concurrent runs.
- Crash, security, dogfood and acceptance projects all passed against immutable builds.

## Exit evidence

```text
e2e-fake: 36 files, 38 passed
crash-matrix: integration 10 passed; crash E2E 1 passed, 1 skipped
security: 4 files passed (2 skipped) + vulnerability 1 passed
dogfood: 7 files, 7 passed
acceptance: 13 files, 13 passed
```

No historical G4 checkpoint is claimed; the current final-tree evidence is reproducible with `pnpm ci:local`.
