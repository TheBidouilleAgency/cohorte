{
  "gate": "G4",
  "scenario": "windows-named-pipe-implementation",
  "status": "implemented-runtime-unverified",
  "checked_at": "2026-09-22T18:24:00Z",
  "implementation": {
    "transport": "windows-named-pipe",
    "network_port_opened": false,
    "remote_clients_rejected": true,
    "acl": "current user SID only",
    "single_service_guard": "Local user-session mutex",
    "frame_limit_bytes": 1048576,
    "protocol": "cohorte/1 JSON-RPC",
    "lifecycle_commands": ["service start", "service status", "service stop"],
    "direct_dependency": "pywin32>=312; sys_platform == 'win32'"
  },
  "automated_test": {
    "file": "tests/integration/test_windows_pipe.py",
    "matrix": ["windows-latest/python-3.12", "windows-latest/python-3.13"],
    "local_result": "platform-skipped on Darwin",
    "required_runtime_assertions": [
      "current SID is available",
      "background service starts",
      "reported transport is windows-named-pipe",
      "protocol health succeeds",
      "RPC shutdown removes the service"
    ]
  },
  "limitations": [
    "No Windows host was available in this session, so this is not Windows support evidence.",
    "Live event follow remains disabled on the Windows pipe until bounded asynchronous writes are validated there.",
    "The external Francois client remains unvalidated."
  ]
}
