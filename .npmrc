# pnpm settings live in pnpm-workspace.yaml. This file only pins where packages come from, so a
# developer's or a CI runner's user-level configuration cannot silently point installs elsewhere.
# It must never hold a credential.
registry=https://registry.npmjs.org/
