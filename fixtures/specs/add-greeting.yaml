id: add-greeting
kind: feature
status: draft
title: Add greeting
acceptance:
  - the frontend exposes a greeting
  - the backend health endpoint remains available
surfaces:
  frontend: { tasks: [expose the greeting] }
  backend: { tasks: [preserve the health endpoint] }
contract: contracts/greeting.md
openQuestions: []
