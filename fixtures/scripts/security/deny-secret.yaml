version: 1
agents:
  - match: {}
    steps:
      - do: tool
        tool: read_file
        input: { path: .env }
      - do: submit
        output: { status: completed, summary: policy denied the secret read, artifacts: [], findings: [], checks: [], questions: [], confidence: 1 }
